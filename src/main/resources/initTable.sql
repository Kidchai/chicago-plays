DROP TABLE IF EXISTS events;

CREATE TABLE events
(
    id          INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title       VARCHAR,
    firstDate   TIMESTAMP,
    theatre     VARCHAR,
    genres      VARCHAR,
    description VARCHAR,
    eventUrl    VARCHAR,
    minPrice    INTEGER,
    maxPrice    INTEGER,
    nextShow    VARCHAR
);