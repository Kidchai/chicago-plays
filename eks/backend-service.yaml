apiVersion: v1
kind: Service
metadata:
  labels:
    io.service: backend
  name: backend
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "alb"
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    io.service: backend
  type: LoadBalancer
status:
  loadBalancer: {}