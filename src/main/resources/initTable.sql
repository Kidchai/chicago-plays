DROP TABLE IF EXISTS events;

CREATE TABLE events
(
    id           INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title        VARCHAR,
    runs         VARCHAR,
    theatre      VARCHAR,
    genres       VARCHAR,
    description  VARCHAR,
    eventUrl     VARCHAR,
    price        VARCHAR,
    nextShow     VARCHAR
);